<div class="bg-black px-4 py-4 space-y-6">
    <% foot03Sections.forEach(section => { %>
      <div>
        <h2 class="text-white font-semibold"><%= section.heading %></h2>
        <p class="text-gray-400 text-sm"><%= section.details %></p>
      </div>
    <% }) %>
  </div>
  